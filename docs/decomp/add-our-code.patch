diff --git a/psx.ld b/psx.ld
index 8712410..c05abec 100644
--- a/psx.ld
+++ b/psx.ld
@@ -219,6 +219,11 @@ SECTIONS
 
         #endif
 
+        /* Spyro 1 Co-Op Mod. Placed LAST so every byte of the game's own code
+           keeps the address it has in retail, and only our code extends the
+           image. */
+        build/src/coop.o(.text .text.*);
+
         . = ALIGN(., 4);
         main_TEXT_END = .;
         main_TEXT_SIZE = ABSOLUTE(main_TEXT_END - main_TEXT_START);
diff --git a/src/main.c b/src/main.c
index 7b0670a..f325214 100644
--- a/src/main.c
+++ b/src/main.c
@@ -13,6 +13,8 @@ int g_StateSwitch;
 int g_DeltaTime;
 int g_UnprocessedFrames;
 
+extern void Sp1x2Frame(void);
+
 void main(void) {
   Initialize();
 
@@ -31,6 +33,8 @@ void main(void) {
 
     g_UnprocessedFrames = 0;
 
+    Sp1x2Frame();
+
     if (!g_StateSwitch) {
       GamestateDraw();
     }
