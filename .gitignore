# NEVER commit ROMs, discs, or extracted game assets — project rule.
Roms/
*.bin
*.cue
*.iso
*.img
*.exe
*.EXE
*.STR
*.WAD
*.wad
*.dat

# Build output and extracted disc contents
mod/projects/*/build/
mod/projects/*/rom/
mod/projects/*/disc/
mod/projects/*/*.xml

# External projects we read but never build — each has its own upstream:
# Spyromain's Spyro2x2, and the three Spyro 1 reverse-engineering efforts.
reference/

# Downloaded binaries — re-fetchable, and the emulator installer is 45 MB
tools/

# macOS
.DS_Store

# Private working notes — correspondence and anything not ours to publish.
# The repository is public; this directory is not.
docs/private/
